# Railway Environment Variables Configuration
# Copy this file to .env for local development (not for Railway - set in dashboard)

# Server Configuration
PORT=8050
HOST=0.0.0.0
DEBUG=false

# Data Storage Paths
# For Railway with volumes, use: /app/data/indicators
# For local development, use: ./data/indicators
INDICATOR_PATH=./data/indicators
BACKTEST_PATH=./data/backtests

# Optional: AWS S3 Configuration (if using cloud storage)
# AWS_ACCESS_KEY_ID=your_access_key
# AWS_SECRET_ACCESS_KEY=your_secret_key
# AWS_S3_BUCKET=your-bucket-name
# AWS_REGION=us-east-1

# Optional: Database Configuration (if using database backend)
# DATABASE_URL=postgresql://user:pass@host:5432/dbname
